import greenfoot.*; 
/**
 * This is the retry button
 * 
 * @Andy, Charmaine, Brice
 * June 2018
 */ 
public class Retry extends Actor
{
    public Retry(){
        GreenfootImage image=getImage();
        image.scale(125,100);
        setImage(image);
    }    
    //create a image of it
}